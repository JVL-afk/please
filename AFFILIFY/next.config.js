/** @type {import("next").NextConfig} */
const nextConfig = {
  // Add any Next.js specific configurations here
  // For example, to enable standalone output for Docker:
  // output: "standalone",
};

module.exports = nextConfig;

